---
description: LIVE and PACKS stay on promoted V3 until the NS V4 legal-topology canary is accepted; then one atomic V4 fabric switch.
alwaysApply: true
---

# Live and PACKS are one fabric

Read `docs/PACK-FACTORY.md` and `docs/ROUTING-FREEZE-2026-09-03.md` before
building or publishing a region. Cursor's Pack Factory skill is an execution aid;
the checked-in repository contract wins on conflict.

**2026-09-06 halt:** Do not stamp, candidate-upload, or promote remaining V3
regions. Public catalog objects stay:

- `dirt-packs/{id}/graph.v3.bin` (or `graph.v2.bin` where not yet V3)
- `dirt-packs/{id}/geometry.v1.bin`
- `dirt-packs/{id}/fuel.v1.json`

The legal-topology mould is **one Nova Scotia `graph.v4.bin` DEV candidate**
under `dirt-packs/v4/candidates/<id>/ns/`. Do not point production
`dirt-mayday.vercel.app` at V4. Do not mix V3 and V4 in one search.

After owner acceptance, every region is built to V4 from one source-epoch,
seams are sealed, and one versioned manifest pointer switches atomically.
V1/V3 remains rollback.

- Foundational regional packs are OSM-only.
- Downloaded versus cellular is delivery, not a second road network.
- Vercel contains API code only. Never include pack binaries.

## When you change routing or packs

| Change | Required release action |
| --- | --- |
| V4 legal-topology (this migration) | Follow `docs/PACK-FACTORY.md`: fixtures green, one NS V4 DEV candidate, owner acceptance, then national V4 — never a silent V3 restamp. |
| Cost, access-as-cost, or search law | Reopens the frozen routing candidate. Not part of Pack Factory. |
| Live loader only | Edit shared pack-fabric source, commit, deploy LIVE, assert affected regions. |

## Forbidden

- Building/promoting remaining V3 regions during the V4 migration.
- Bare `--pack` publication.
- Replacing the remote catalog with the checked-in seed manifest.
- Auto-downloading packs merely to make LIVE match.
- Longhaul or a second live extract.
- Provincial supplements in a foundational OSM-only build.
- A router-cost/search change hidden inside Pack Factory.
- Runtime proximity stitches on V4.
- Coordinate equality as topology identity.

Workspace: `/Users/richardsmith/SandBox01/MAYDAYiOS/Dirt`

API: `https://dirt-mayday.vercel.app/api/route`

## September 7 routing-evolution qualification

Use only sealed DEV V4 release `fabric-v4-20260907-01`. Do not rebuild, modify,
upload, restamp, or substitute its packs; do not use V3 fallback, runtime OSM,
production, or a physical phone during automated qualification.

Routing search, costs, variety seeds, forward progress, retrace rejection, and
fuel-replacement ranking are one contract. Change JavaScript and Swift in the
same change and prove a non-zero seed produces identical legal geometry. A
live-only or phone-only search change is a defect. The production and DEV live
services must not silently diverge from the downloaded-pack laws.
