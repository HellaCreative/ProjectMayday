"use strict";
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict'),{spawnSync}=require('node:child_process');
const {summarizeSurface}=require('../routing/lib/adventure/surface'),{auditRideShape}=require('../routing/lib/adventure/ride-shape-audit'),{createBudget}=require('../routing/lib/adventure/budget');
const deployment=process.env.REBUILD_DEPLOYMENT;if(!deployment)throw Error('REBUILD_DEPLOYMENT required');
const output=process.env.REBUILD_LIVE_OUTPUT||'routing/candidates/rebuild-device-live';fs.mkdirSync(output,{recursive:true});
const cases=[...(process.env.REBUILD_FINAL_FUEL?[{id:"clean-final-fuel",profile:"cleanest",locations:[{lat:44.764793,lon:-63.340250},{lat:47.047134,lon:-64.891699}],zoom:9.6}]:[]),...(process.env.REBUILD_NS_CLEAN?[{id:'ns-clean-cape',profile:'cleanest',locations:[{lat:44.764835,lon:-63.340265},{lat:46.214698,lon:-59.965492}],zoom:11.6}]:[]),...(process.env.REBUILD_NS_HOOKS?[{id:'ns-hooks',profile:'dirt',locations:[{lat:44.764835,lon:-63.340265},{lat:46.214698,lon:-59.965492}],zoom:11.6}]:[]),...(process.env.REBUILD_NB_SHORT?[{id:'nb-short-unknown',profile:'dirt',locations:[{lat:46.646799,lon:-64.87533},{lat:47.762610,lon:-65.856301}],zoom:10.3,unknown:true,usable:162000}]:[]),...(process.env.REBUILD_SHORT_REVERSE?[{id:'short-reverse-balanced-unknown',profile:'balanced',locations:[{lat:47.762610,lon:-65.856301},{lat:44.764834,lon:-63.340240}],zoom:10.3,unknown:true,usable:162000}]:[]),{id:'moved',profile:'dirt',locations:[{lat:44.764810,lon:-63.340233},{lat:47.743529,lon:-64.911236}],zoom:8.6},{id:'near-dalhousie',profile:'dirt',locations:[{lat:44.764810,lon:-63.340233},{lat:47.914772,lon:-65.948078}],zoom:9.8},{id:'dalhousie',profile:'dirt',locations:[{lat:44.764831,lon:-63.340263},{lat:47.986597,lon:-66.328424}],zoom:11.2},{id:'clean-phone',profile:'cleanest',locations:[{lat:44.76484254584986,lon:-63.34021846556175},{lat:46.792506,lon:-67.569371}],zoom:10.7}],summary=[];
for(const c of cases) {
 if(process.env.REBUILD_FINAL_FUEL&&c.id!=="clean-final-fuel")continue;
 if(process.env.REBUILD_NS_CLEAN&&c.id!=='ns-clean-cape')continue;
 if(process.env.REBUILD_NS_HOOKS&&c.id!=='ns-hooks')continue;
 if(process.env.REBUILD_NB_SHORT&&c.id!=='nb-short-unknown')continue;
 if(process.env.REBUILD_SHORT_REVERSE&&c.id!=='short-reverse-balanced-unknown')continue;
 const usable=c.usable||225000;
 const body={profile:c.profile,locations:c.locations,accessPolicy:{motorizedPermissive:true,motorizedUnknown:c.unknown===true},options:{mapZoom:c.zoom,avoidMotorways:c.profile==='cleanest'},fuel:{usableRangeMeters:usable,firstLegMaxMeters:usable,minimumFuelStops:0,windowMaxStops:c.id.startsWith('ns-')?4:12,allowPartialWindow:true,windowTimeBudgetMs:20000,routeFirstPlan:true,ensureDestinationFuelEscape:true,forwardFeeler:false}};
 const file=path.resolve(output,c.id+'-request.json');fs.writeFileSync(file,JSON.stringify(body));const at=Date.now();
 const run=spawnSync('vercel',['curl','/api/fuel-chain','--deployment',deployment,'--','--silent','--show-error','--max-time','30','--request','POST','--header','Content-Type: application/json','--data-binary','@'+file],{encoding:'utf8',timeout:45000,maxBuffer:30*1024*1024});
 fs.writeFileSync(path.join(output,c.id+'-response.json'),run.stdout||'');assert.equal(run.status,0,run.stderr);const r=JSON.parse(run.stdout);
 assert.equal(r.status,'complete',r.message);assert.equal(r.diagnostics.strategy,'adventure-preview-v1');assert.equal(r.windowComplete,true);assert.equal(r.routes.length,r.stops.length+1);assert.ok(r.packIdentity.every(p=>p.releaseId==='fabric-v4-20260908-02'));
 for(let i=0;i<r.routes.length;i++){assert.ok(r.routes[i].distanceMeters<=usable+1e-6);if(i)assert.deepEqual(r.routes[i].geometry[0],r.routes[i-1].geometry.at(-1));}
 assert.ok(r.routes.at(-1).distanceMeters+r.destinationEscapeMeters<=usable+1e-6);
 const segments=r.routes.flatMap(r=>r.segments),surface=summarizeSurface(segments),shape=auditRideShape({segments,budget:createBudget({deadlineAtMs:Date.now()+10000,maxExpansions:1000000})});
 assert.equal(shape.state,'complete');if(c.id==='clean-final-fuel'){assert.equal(shape.repeatedRoadMeters,0);assert.ok(surface.pavedPercent>99.5);assert.equal(r.diagnostics.selectedReason,'paved');assert.equal(r.stops.length,3);}if(c.id==='ns-clean-cape'){assert.ok(surface.pavedPercent>99);assert.equal(shape.repeatedRoadMeters,0);assert.equal(r.diagnostics.selectedReason,'paved');}if(c.id==='ns-hooks'){assert.equal(shape.repeatedRoadMeters,0);assert.ok(surface.knownDirtPercent>65);assert.equal(shape.dirtRuns.filter(run=>run.startMeters<20000).length,0);}if(['moved','near-dalhousie','dalhousie'].includes(c.id)){assert.ok(shape.repeatedRoadMeters<2000);assert.ok(surface.knownDirtPercent>60);}
 if(process.env.REBUILD_STRICT_NB_REPEAT==='1'&&c.id==='near-dalhousie')assert.ok(shape.repeatedRoadMeters<=471+1e-5,'near-Dalhousie must not exceed its qualified 471 m repeated-road baseline');
 assert.equal(r.diagnostics.adventure.search.poolComplete,true);assert.ok(r.diagnostics.adventure.candidates.every(c=>c.fuel==='provisional_station_access'));
 const classes={};for(const s of segments)classes[s.roadClassLeaf]=(classes[s.roadClassLeaf]||0)+s.distanceMeters;
 if(process.env.REBUILD_CLEAN_BACKROADS&&c.id==='clean-phone'){assert.ok(surface.pavedPercent>99.9);assert.equal(shape.repeatedRoadMeters,0);assert.ok(['primary','primary_link','trunk','trunk_link'].reduce((n,k)=>n+(classes[k]||0),0)<250000);}
 const row={id:c.id,build:r.serviceBuild,httpIncludingCliMs:Date.now()-at,serverMs:r.debug.adventureTotalMs,km:surface.distanceMeters/1000,dirt:surface.knownDirtPercent,repeatKm:shape.repeatedRoadMeters/1000,stops:r.stops.map(s=>({id:s.id,address:s.address})),classes};summary.push(row);console.log(JSON.stringify(row));
}
fs.writeFileSync(path.join(output,'summary.json'),JSON.stringify({deployment,summary},null,2));
