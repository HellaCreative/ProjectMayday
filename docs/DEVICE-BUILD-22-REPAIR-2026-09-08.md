# DIRT Dev 2 (22) — White build repair

Richard explicitly requested repairing the current Xcode build and installing it on White, superseding the earlier no-install direction for this repair.

Source: the project open in Xcode at `/Users/richardsmith/SandBox01/MAYDAYiOS/Dirt/Dirt.xcodeproj`, on `feature/routing-itinerary-rebuild`, with pre-existing uncommitted routing and pack work preserved. This is not the isolated routing-rebuild worktree or the earlier rollback archive. No server or pack publication occurred.

## Changes

- Mark the read-only customer endpoint helper nonisolated so the background router can call it with the project's MainActor default.
- Remove one stray `roadAway(state, toState)` call in the legacy node search, where neither the helper nor those state variables exist. Preserve the existing geographic approach cost and other pre-existing experiments.
- Correct both development StoreKit references to the relative path generated by Xcode, and correct the development verifier's expectation.
- Advance only the Debug application build number to 22; version remains 2.

## Verification

- Physical-device Debug build succeeded with Xcode's toolchain.
- All eight development bundle/configuration checks passed, including DEV service isolation and StoreKit reference resolution.
- Xcode Run installed the app and displayed `Running Dirt on white`.
- Device inventory independently confirmed `com.mayday.dirt.dev`, version 2, build 22.
- `git diff --check` passed.

This verifies compilation, installation and launch, not routing acceptance or offline parity of the experimental engine. The source already contained unfinished native compass/retrace changes before this repair; this build does not qualify those behaviors. No device routing packs were downloaded by this work.

Android counterpart: none for these compiler isolation, Xcode scheme, verifier and development build-number corrections. No new shared product behavior is defined. Existing Android routing acceptance requirements remain open.
