# Claude — 2026-08-22 14:54 ADT — turn-1 verdict + turn-2 plan

## Turn 1 verdict: REJECTED (Direct cross-track tax)

- Cursor raised Direct `k` 0.018 → 0.036. **0 of 5 Direct-overshoot cases
  converted**; two even grew (`dartmouth-capebreton/direct/fuel-off` 424→428 km).
- Bench measured **40/65** (git `a965d91`) but the **true durable baseline is
  38**. The +2 are `dartmouth-capebreton/balanced/fuel-on` and
  `three-waypoint/balanced/fuel-on` — both flipped on the `ms/hop` timing
  assertion, and `k` is a Direct-only knob (Balanced uses `k=0.014`, untouched),
  so it cannot green a Balanced route. Timing noise, not progress.
- **Blocker fixed this turn:** the uncommitted Phase-11 fuel-chain WIP imported
  `../regional/endpoint-resolver`, a module that exists only on Codex's discarded
  branch — it broke the whole suite (8 test failures, bench couldn't start).
  Shelved to `git stash@{0}` and `@{1}` (recoverable). **Do NOT `git stash pop`
  them into this loop** — they re-break the bench. `npm test` now 58 pass / 0 fail.

## Turn 2 hypothesis

Direct overshoots (27–37% over shortest) because it prefers longer minor/unpaved
roads that run *along* the corridor — NOT lateral excursion (turn 1 proved the
cross-track tax can't fix it). Two candidate levers; we must know which dominates
before tuning:

- **Surface table:** `paved 1.15 > gravel 1.00 > track 0.90` → takes ~28%-longer dirt.
- **Road-class table (DIRECT_ROAD_CLASS_WEIGHTS):** `arterial 1.45 / freeway 1.7`
  vs `local 0.98 / track 0.9` → avoids the fast direct highway for minor roads.

## Step A — Codex diagnostic (review only, NO code change)

Trace `antigonish-sydney/direct/unknown-off/fuel-off` (279.0 km vs 220 km
shortest, +27%, clean mid-size case, no fuel/no timeout confounds). Determine
where the extra ~59 km comes from:

- Is the chosen path taking **unpaved** roads a paved shorter road parallels
  (→ surface table dominates)? Or
- Is it avoiding an **arterial/freeway** that is the near-shortest direct line
  (→ road-class table dominates)?

Report the dominant factor + which single table to flatten. Set Next agent: Cursor.

## Step B — Cursor (one knob)

1. Revert Direct `k` 0.036 → **0.018** (turn 1 cleanup — it was inconclusive and
   slightly negative). JS + Swift lockstep.
2. Flatten the ONE table Codex identified toward neutral **for Direct only**
   (surface → paved≈gravel≈track≈1.0, OR road-class → arterial/freeway toward
   ~1.0). Do NOT touch both. Balanced/Dirt/Clean unchanged.
3. `npm run bench:ns` locally (no deploy), update SCOREBOARD, write inbox note,
   hand to Codex for the safety gate.

## Success check

- Convert Direct-overshoot reds (`dartmouth-capebreton`, `dartmouth-antigonish`,
  `antigonish-sydney` direct fuel-off/on) toward green (`length ≤ shortest+15km`).
- Protected greens must not flip: `short-no-fuel/direct/unknown-off/fuel-off`,
  `dartmouth-antigonish/direct/unknown-off/fuel-on`,
  `musq-sherbrooke/direct/unknown-off/fuel-off`,
  `musq-sherbrooke/direct/unknown-off/fuel-on`, and every non-Direct green.
- Ignore Balanced-fuel-on timing flips as progress — judge on Direct length only.
