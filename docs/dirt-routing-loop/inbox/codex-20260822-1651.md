# Codex verify — 2026-08-22 16:51 ADT — spec alignment

**Verdict: PASS. Accept 38/65 as the new spec-aligned baseline.**

## Safety gate

- **`npm test`: PASS.** 59 passed, 0 failed, 1 intentionally skipped.
- **Fresh `npm run bench:ns`: PASS.** Promoted pack
  `ns-osm-20260821-02`; **38/65** reproduced exactly. Cursor's run and the
  independent run have identical case statuses and route metrics; only runtime
  measurements varied.
- **Direct costs vs HEAD: PASS.** Both
  `scripts/pack-fabric/routing/lib/profile-costs.js` and
  `Dirt/Routing/OnDevice/OnDeviceProfileCosts.swift` are byte-identical to HEAD.
  The turns 1–4 cost experiments are absent: Direct is again dirt-biased and
  highway-avoiding, with its HEAD surface/road-class tables,
  `majorHighwayAvoidMult`, and `k = 0.018`.

## Corridor lockstep

JS and Swift match:

| Mode | JS `hop-search.js` | Swift `HopSearchPolicy.swift` |
| --- | ---: | ---: |
| Direct | 25,000 m | 25,000 m |
| Balanced | 25,000 m | 25,000 m |
| Dirt base | 60,000 m | 60,000 m |
| Dirt maximum | 80,000 m | 80,000 m |

Search behavior also matches across `find-path-v2.js` and
`OnDeviceRouter.swift`:

- Direct and Balanced run one hard 25 km envelope.
- Dirt runs 60 km first and may run 80 km only if 60 km produced no candidate.
- All attempted widths are capped; no Direct/Balanced/Dirt unbounded corridor
  attempt remains. Other uses of numeric infinity in search arrays/progress
  helpers do not create an unbounded corridor envelope.

The new JS and Swift corridor tests pass.

## Direct assertion

The retired `length <= shortest + 15 km` Direct assertion is absent. Completed
Direct cases are evaluated by both:

- dirt >= 60%; and
- maximum cross-track <= 25 km.

Every completed Direct case in the fresh benchmark passes the corridor
assertion. Direct reds are now low dirt or `no_route`, consistent with the new
mode spec.

## Drift / concerns

No functional drift found. The 38-case drop from the earlier experimental
41/39 state is the explicitly authorized spec-alignment rebaseline, not a failed
one-knob turn. One-knob rules resume now against this SCOREBOARD.

No stash operation, deployment, product change, pack, or manifest edit was
performed by Codex.

**Next agent: Claude.** Plan the next single-knob turn against the new 38/65
SCOREBOARD.
