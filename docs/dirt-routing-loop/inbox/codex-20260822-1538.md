# Codex verify — 2026-08-22 15:38 ADT

**Verdict: PASS. Keep the Direct highway exemption; no revert.**

## Safety gate

- **JS↔Swift lockstep: PASS.** In both
  `scripts/pack-fabric/routing/lib/profile-costs.js` and
  `Dirt/Routing/OnDevice/OnDeviceProfileCosts.swift`:
  - Direct `majorHighwayAvoidMult` returns `1`.
  - Direct freeway / arterial / ramp road-class weights are `1.0`.
  - Direct corridor cross-track `k` is `0.018`.
  - Balanced, Dirt, and Clean highway avoidance and road-class tables are
    unchanged by this patch.
- **`npm test`: PASS.** 58 passed, 0 failed, 1 intentionally skipped. The first
  sandboxed run could not read the NS graph and returned five integration-route
  errors; the authoritative rerun with read-only promoted-graph access passed.
- **Fresh `npm run bench:ns`: PASS.** Promoted pack
  `ns-osm-20260821-02`; **40/65 measured, 39 durable**.

## Durable result and protected greens

The new Direct conversion reproduced:

- `antigonish-sydney/direct/unknown-off/fuel-on`: **234.8 km, green** against
  the 234.9 km limit.

All protected Direct greens reproduced:

- `short-no-fuel/direct/unknown-off/fuel-off`: **7.4 km, green**.
- `dartmouth-antigonish/direct/unknown-off/fuel-on`: **233.0 km, green**.
- `musq-sherbrooke/direct/unknown-off/fuel-off`: **55.1 km, green**.
- `musq-sherbrooke/direct/unknown-off/fuel-on`: **55.1 km, green**.

The remaining Direct-overshoot routes also reproduced Cursor's shortened but
red lengths exactly: Antigonish–Sydney fuel-off 253.7 km,
Dartmouth–Antigonish fuel-off 297.3 km, and Dartmouth–Cape Breton fuel-off/on
374.0 / 384.0 km.

No durable/former green flipped red. The only run-to-run status changes were the
known timing-sensitive Balanced fuel-on pair: Dartmouth–Cape Breton changed red
to green, while three-waypoint changed green to red at 4095 ms/hop. They offset
at 40 measured and are not routing progress or regression. The durable count is
therefore 39: prior durable 38 plus the reproduced Direct conversion.

No stash operation, deployment, pack/manifest edit, next-knob implementation,
or Direct exemption revert was performed.

**Next agent: Claude.** Turn result: 1/5 target Direct overshoots converted;
four remain red despite meaningful length reductions.
