# Claude plan — 2026-08-22 14:14 ADT — first turn, 38 → 45

**Blocked by GATE (branch consolidation). Do not execute until GATE: CLOSED.**

## Cluster chosen (one)

**Direct overshoot** — Direct routes exceed `shortest + 15 km`. Cleanest
single-knob cluster; five red cases, all the same failure mode.

Red case IDs:

1. `antigonish-sydney/direct/unknown-off/fuel-on` — 237293 m vs 219886 m (only ~2.4 km over the +15 km line; easiest)
2. `antigonish-sydney/direct/unknown-off/fuel-off` — 277832 m vs 219886 m
3. `dartmouth-antigonish/direct/unknown-off/fuel-off` — 310292 m vs 228531 m
4. `dartmouth-capebreton/direct/unknown-off/fuel-off` — 424859 m vs 320735 m
5. `dartmouth-capebreton/direct/unknown-off/fuel-on` — 431644 m vs 320735 m

(NOT in this cluster: `through-halifax/direct/*` fails as `no_route` — that is the
connectivity cluster, a separate turn.)

## Hypothesis (one sentence)

Direct wanders off the crow-flies line to collect gravel/track because its
surface table rewards unpaved (`paved 1.15 > gravel 1.00 > track 0.90`) while the
corridor cross-track tax (`directCrossTrackExtra` k=0.018, quadratic) is too weak
to hold the line over long routes — so length runs past shortest + 15 km.

## Allowed files (one knob, one patch)

- `scripts/pack-fabric/routing/lib/profile-costs.js`
- `Dirt/Routing/OnDevice/OnDeviceProfileCosts.swift` (lockstep if the knob exists there)

**Primary knob:** raise Direct's cross-track corridor tax
(`directCrossTrackExtra`, the `direct` branch `k`) so off-line distance is
penalized harder. This targets "off-line overshoot" without changing Direct's
surface *character*.

**Fallback (only if primary regresses a green — revert first, do not stack):**
flatten the Direct surface table toward neutral so paved is not penalized vs
gravel/track. Second knob = a new turn, not the same one.

## Success check

- Reds → green: all five above must satisfy `length ≤ shortest + 15 km`.
  Expect +2 to +5 (case 1 is nearly there; case 4/5 are the tallest and may need
  the fallback next turn).
- Greens that MUST NOT flip:
  - `short-no-fuel/direct/unknown-off/fuel-off`
  - `dartmouth-antigonish/direct/unknown-off/fuel-on`
  - `musq-sherbrooke/direct/unknown-off/fuel-off`
  - `musq-sherbrooke/direct/unknown-off/fuel-on`
  - and every non-Direct green (Direct knobs should not touch them, but re-bench proves it).
- Per-turn safety gate (CONTRACT): `npm test` passes; JS↔Swift lockstep holds.

## Next agent

**Cursor** — implement the one primary knob, `npm run bench:ns`, update
SCOREBOARD, write `inbox/cursor-*.md`, then hand to **Codex** for the
post-execution verify/debug pass (runs the CONTRACT safety gate). If green count
drops or any listed green flips red → revert and hand back to Claude. Do not fix
forward.
