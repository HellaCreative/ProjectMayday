# Codex verification — PASS

Date: 2026-08-22 17:27 ADT

Cursor's Balanced surface-live Swift compile fix passes independent verification.

## Results

- `xcodebuild ... build-for-testing` for Dirt on iPhone 17 / iOS 26.5 with signing disabled: **TEST BUILD SUCCEEDED**.
- `npm test`: **59 passed, 1 skipped, 0 failed**.
- `npm run bench:ns`: **38/65 green**, seed `3511091208`, result `a965d91-20260822T202554Z.json`.
- Compared with Cursor's `a965d91-20260822T202158Z` run: the green/red case set and route measurements are identical. Only runtimes changed; no green flipped red.
- Guards held: Dartmouth–Cape Breton Balanced fuel-off **49%**; Antigonish–Sydney Balanced fuel-off **52%**.
- Remaining targets are unchanged: Musquodoboit–Sherbrooke Balanced is **40%** and short-no-fuel Balanced is **44%**.

## Lockstep

- Swift ordinary-edge `surfMult` is now one immutable ternary assignment, so the prior reassigned-`let` compile error is gone.
- JS ordinary and virtual Balanced edges multiply length by `surfaceMultiplier(..., "balanced")`.
- Swift ordinary and virtual Balanced edges multiply length by `OnDeviceProfileCosts.surfaceWeight(profile: .balanced, ...)` only for `.balanced`; `hopCostStep` does the same for `.balancedResource` only when the profile is Balanced.
- `profile-costs.js` and `OnDeviceProfileCosts.swift` have no diff from HEAD, so the existing surface table did not drift.

## Concern

None blocking. This rerun had timing-only differences on already-red fuel cases, but the result set and routing measurements were stable.

## Handoff

Next agent: **Claude**. Plan the next one-knob turn to tune Balanced into the 45–55% band, with Musquodoboit–Sherbrooke still at 40%.
