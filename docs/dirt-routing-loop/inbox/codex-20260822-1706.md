# Codex verify — 2026-08-22 17:06 ADT — Balanced surface live

**Verdict: FAIL — Swift lockstep is not buildable. Revert this one-knob turn.**

## Required checks

- **`npm test`: PASS.** 59 passed, 0 failed, 1 intentionally skipped.
- **Fresh `npm run bench:ns`: PASS on JS behavior.** Promoted pack
  `ns-osm-20260821-02`; **38/65** reproduced. No case status flipped relative
  to Cursor's run.
- **Cost-table drift: PASS.** `profile-costs.js` and
  `OnDeviceProfileCosts.swift` remain byte-identical to HEAD; all surface and
  road-class table numbers are unchanged.
- **JS behavior: PASS.** `searchBalancedResource` multiplies ordinary and
  virtual-edge length steps by the existing Balanced `surfaceMultiplier`.
- **Swift lockstep/build validity: FAIL.** In
  `Dirt/Routing/OnDevice/OnDeviceRouter.swift`, the ordinary-edge branch of
  `searchVirtualBalanced` declares `let surfMult = 1.0` and then assigns
  `surfMult = OnDeviceProfileCosts.surfaceWeight(...)` when the profile is
  Balanced. Swift constants cannot be reassigned, so this source does not
  compile. The virtual-edge branch correctly uses `var`, and `hopCostStep`
  correctly gates the same formula to `.balanced`, but the ordinary-edge error
  is sufficient to fail the JS↔Swift safety gate.

## Benchmark details

The JS-side claims reproduced exactly:

- Dartmouth–Cape Breton Balanced fuel-off: **49%**, guard <=55 held.
- Antigonish–Sydney Balanced fuel-off: **52%**, guard <=55 held.
- Short-no-fuel Balanced: **44%**, still red but closer.
- Musquodoboit–Sherbrooke Balanced fuel-off/on: **40%**, unchanged.
- Dartmouth–Antigonish Balanced fuel-off/on: **54%**, still in band.
- Three-waypoint Balanced fuel-off/on: **51.2% / 49.2%**, still in band.

Dirt, Direct, and Clean statuses and route metrics match the independent
spec-alignment baseline; differences are runtime measurements only. No former
green flipped in the Node benchmark.

## Required hand-back

Revert the Balanced surface-live additions in `find-path-v2.js` and
`OnDeviceRouter.swift` as one unit, restoring the verified 38/65 spec-alignment
baseline. Do not fix forward in this failed turn. After revert, Claude may plan
the next one-knob attempt for Musquodoboit–Sherbrooke, where
`pickResourceEnd` still prefers the 40% label because no in-band destination
label exists.

Codex made no product-code change, stash operation, deployment, pack, or
manifest edit.

**Next agent: Claude — revert required.**
