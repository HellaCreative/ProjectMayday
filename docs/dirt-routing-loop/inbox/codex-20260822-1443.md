# Codex verify — 2026-08-22 14:43 ADT

**Verdict: FAIL**

## Checks

- **JS↔Swift cost lockstep: PASS.** Direct corridor `k` is `0.036` in
  `scripts/pack-fabric/routing/lib/profile-costs.js`
  (`directCrossTrackExtra`) and
  `Dirt/Routing/OnDevice/OnDeviceProfileCosts.swift`
  (`corridorCrossTrackExtra`). Balanced remains `0.014`; Dirt remains `0.005`.
- **`npm test`: FAIL.** 41 passed, 8 failed, 1 skipped. The primary blocker is
  `MODULE_NOT_FOUND: ../regional/endpoint-resolver`, imported at
  `scripts/pack-fabric/routing/lib/fuel-chain.js:30`. Three test files fail to
  load through that import, and five Nova Scotia route-objective assertions
  return `error` instead of `complete`.
- **Fresh `npm run bench:ns`: FAIL before case execution** with the same missing
  `../regional/endpoint-resolver` module. It used the promoted stable NS pack
  `ns-osm-20260821-02` but produced no fresh case results.
- **Former-green protection: UNVERIFIED.** Because the fresh bench did not run,
  Cursor's reported `40/65` and “no former green flipped red” cannot be
  independently confirmed.
- **Balanced fuel-on +2: UNVERIFIED / not a real baseline yet.** The two cases
  `dartmouth-capebreton/balanced/unknown-off/fuel-on` and
  `three-waypoint/balanced/unknown-off/fuel-on` cannot be shown to stay green on
  a second run. Treat the reproducible baseline as **38/65** unless a clean
  rerun reproduces them; Cursor already identified their gains as hop-time / machine-load noise, not an effect of the Direct knob.

## Concern / one hypothesis

The current worktree restores a dirty `routing/lib/fuel-chain.js` that requires
an absent `routing/regional/endpoint-resolver`. Cursor temporarily ran its bench
against the HEAD fuel-chain and then restored the WIP, so Codex cannot verify
that result in the handed-off state. Reconcile or restore that single missing
dependency/state first, then rerun the safety gate. No bulk fix and no Vercel
deploy were performed.

The Direct-overshoot hypothesis remains unsupported: Cursor reported 0/5 target
cases converted. The Direct `k` patch was not reverted because the verification
failure is caused by pre-existing fuel-chain WIP outside this knob, and reverting
the knob would not make the handed-off worktree testable.

**Next agent: Claude.** Not ready for Rick's ride test.
