# DIRT contract to preserve and test

Reference executable:private repairbf8f740 plus parked preparation571123c, not a claim that every aspirational product rule is globally solved today. Canonical product source docs/00-PRODUCT-AND-ROUTING-SOURCE-OF-TRUTH.md and current live-canary/adventure code must both be considered. Ordinary engine road results are a separate benchmark class.

- Dirt ranks known unpaved character;Balanced ranks proximity to50/50;Clean minimizes dirt and uses pavement where feasible. Existing implementation creates additive paved/dirt10/dirt30 candidates (continuations add mixed2) and ranks a finite feasible pool;it does not prove global optimal dirt fraction. Do not replace this with fastest-car routing silently.
- Wander in[0,1] adds30*(1-wander)^2 distance charge while preserving profile objectives. City/highway avoidance and return-road preferences need explicit treatment, no implicit dropping.
- Access precedence motorcycle>motor_vehicle>vehicle>access. Explicit private/purpose-limited access is not through access. Unknown motorized access requires Allow Unknown;Clean forces it off. Snapped endpoint exceptions must not permit illegal travel direction. Surface unknown remains distinct from dirt.
- Directed edges,barriers,node and via-way restrictions,exact cross-region connectivity and overlap aliases. Proximity is not a seam. Engine import eligibility may differ and must be disclosed/audited.
- Ordered rider waypoints and per-leg profile/access settings;refinement must preserve legality/fuel and report deadline-limited alternatives.
- Fuel usable range=full range*(1-reserve);starting usable fuel may be less than full. Every hop must use actual traversable road distance,not straight line. Stop/escape proof,station identity,exclusions,continuation arrival direction and restriction history must survive replanning. Mapped station proximity is provisional access,not physical entrance proof.
- Existing history is bounded to256edges/30km;restriction-compatible continuous suffix matters. Test a fuel stop on a junction and on a split edge,no invented U-turn.
- Failure/cancellation must distinguish unproved/limited from no path;an unfuelled or incomplete alternative result cannot count as full DIRT success.

Qualification tiers: R=ordinary road latency/continuity;P=surface/access/preference prototype;F=range/station/continuation proof;C=mixed concurrent qualifiedrequests. Record unsupported constraints per engine.
